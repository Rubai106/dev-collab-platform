# Optional for split deployments such as Vercel frontend + Render backend.
# Leave blank when the Express server serves the built client from the same origin.
VITE_API_URL=

# Optional. If omitted, the client derives the Socket.IO origin from VITE_API_URL.
VITE_SOCKET_URL=

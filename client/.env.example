# Backend API URL for split deployments (Vercel frontend + Render backend)
VITE_API_URL=https://dev-collab-platform-eme5.onrender.com/api

# Optional. If omitted, the client derives the Socket.IO origin from VITE_API_URL.
VITE_SOCKET_URL=
